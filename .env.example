# PostgreSQL database connection string
DATABASE_URL='Render database URL'

# Secret key used to sign JWT tokens (a long random string)
JWT_SECRET='long jwt secret'

# Gmail address used to send verification emails
EMAIL_USER='the email address used to send verification emails'

# Gmail App Password (generated from your Google Account)
EMAIL_PASS='app_password'

# Frontend base URL (changed to Vercel domain when deployed)
CLIENT_URL=http://localhost:5173

# Backend base URL (changed to Render domain when deployed)
SERVER_URL=http://localhost:5000

# Set to 'development' or 'production'
NODE_ENV='development'
